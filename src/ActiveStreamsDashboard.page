<style>
/* ---------------------------------------------------------
   CONTAINER CONTEXT
--------------------------------------------------------- */
#activestreams_streams {
    width: 100%;
    container-type: inline-size;
    container-name: as-widget;
}

/* ---------------------------------------------------------
   GRID LAYOUT
   1. Server Dot (24px)
   2. Name (1fr)
   3. Device (Hidden/0px)
   4. User (Hidden/0px)
   5. Time/Status (190px)
--------------------------------------------------------- */
.as-header-row,
.as-row {
    display: grid; 
    grid-template-columns: 24px 1fr 0px 0px 190px;
    align-items: center;
    gap: 10px;
    padding: 8px 12px;
    border-bottom: 1px solid var(--border-color, #e5e5e5);
    box-sizing: border-box;
}

#activestreams_streams .as-row:last-child {
    border-bottom: none;
}

/* ---------------------------------------------------------
   SERVER DOT COLUMN (The colored indicator)
--------------------------------------------------------- */
.as-server {
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: 10px; /* Adjust dot size if it uses FontAwesome */
}

/* --- SERVER BRAND COLORS --- */
/* These assume your PHP adds classes like 'plex', 'emby', or 'jellyfin' 
   to the icon or span inside .as-server */

/* Plex (Orange) */
.as-server .plex, 
.as-server .fa-plex,
.plex {
    color: #e5a00d; 
}

/* Emby (Green) */
.as-server .emby, 
.as-server .fa-emby,
.emby {
    color: #52b54b; 
}

/* Jellyfin (Purple/Blue) */
.as-server .jellyfin, 
.as-server .fa-jellyfin,
.jellyfin {
    color: #aa5cc3; /* Default Jellyfin Purple */
}

/* ---------------------------------------------------------
   DATA COLUMNS
--------------------------------------------------------- */
.as-name {
    font-weight: 600;
    font-size: 0.95rem;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    min-width: 0;
}

.as-device,
.as-user {
    color: var(--text-muted, #888);
    font-size: 0.85em;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    visibility: hidden; 
}

/* Time/Status Column */
.as-time {
    text-align: right;
    font-size: 0.85em;
    font-feature-settings: "tnum";
    color: var(--nice-blue, #2077b4);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    
    display: flex; 
    justify-content: flex-end;
    align-items: center;
    gap: 8px; 
}

/* ---------------------------------------------------------
   HEADER ROW
--------------------------------------------------------- */
.as-header-row {
    background-color: var(--table-header-bg, rgba(0,0,0,0.03));
    height: 32px;
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    color: var(--header-text-color, #666);
}

/* Dark Theme Header Fix */
body.skin-black .as-header-row {
    background-color: rgba(255, 255, 255, 0.1);
    color: #fff;
    opacity: 0.9;
}

/* ---------------------------------------------------------
   CONTAINER QUERIES (Responsive Logic)
--------------------------------------------------------- */

/* NARROW (<450px): Shrink Time column to save Title space */
@container as-widget (max-width: 450px) {
    .as-header-row,
    .as-row {
        grid-template-columns: 24px 1fr 0px 0px 100px;
    }
}

/* MEDIUM (>400px): Show User */
@container as-widget (min-width: 400px) {
    .as-header-row,
    .as-row {
        grid-template-columns: 24px 1fr 0px 80px 190px; 
    }
    .as-user { visibility: visible; }
}

/* WIDE (>650px): Show Device + User */
@container as-widget (min-width: 650px) {
    .as-header-row,
    .as-row {
        grid-template-columns: 24px 1fr 100px 100px 190px; 
    }
    .as-device, .as-user { visibility: visible; }
}

/* ---------------------------------------------------------
   UTILITIES
--------------------------------------------------------- */
.as-row img {
    border-radius: 4px;
    max-height: 24px;
    max-width: 24px;
    object-fit: contain;
    display: block;
}

.as-scrollable {
    display: block;
    overflow: hidden;
    mask-image: linear-gradient(to right, transparent 0%, black 5%, black 95%, transparent 100%);
}
.as-scroll-inner.as-overflowing {
    animation: as-scroll-text var(--scroll-duration, 10s) linear infinite alternate;
}
@keyframes as-scroll-text {
    0%, 15% { transform: translateX(0); }
    85%, 100% { transform: translateX(var(--scroll-distance, -50px)); }
}
</style>
